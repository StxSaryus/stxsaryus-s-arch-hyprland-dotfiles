# Installation Guide — StxSaryus Arch Hyprland Dotfiles

One script. Full rice. NVIDIA Pascal handled per **official Arch Linux news**.

---

## Quick Start (recommended)

```bash
git clone https://github.com/StxSaryus/stxsaryus-s-arch-hyprland-dotfiles.git ~/dotfiles
cd ~/dotfiles
chmod +x install.sh
./install.sh
sudo reboot
```

`./install.sh` shows official notices, detects your GPU, installs packages + yay + configs,
and sets up NVIDIA correctly. Press **Enter** once to confirm (or use `./install.sh --auto`).

After reboot:

```bash
nvidia-smi          # should list your GPU
```

---

## Official notices (embedded in the installer)

### 1) NVIDIA 590 dropped Pascal — Arch Linux News

Source:  
https://archlinux.org/news/nvidia-590-driver-drops-pascal-support-main-packages-switch-to-open-kernel-modules/

> With the update to driver version 590, the NVIDIA driver no longer supports
> Pascal (GTX 10xx) GPUs or older. […] Users with GTX 10xx series and older
> cards must switch to the legacy proprietary branch:
>
> - Uninstall the official `nvidia`, `nvidia-lts`, or `nvidia-dkms` packages  
> - Install `nvidia-580xx-dkms` from the AUR

**What this installer does**

| GPU generation | Driver package |
|----------------|----------------|
| Pascal / Maxwell / older (e.g. GTX 1050 Ti) | AUR `nvidia-580xx-dkms` + `nvidia-580xx-utils` |
| Turing+ (RTX 20xx, GTX 16xx+) | Official `nvidia-open` + `nvidia-utils` |

Also installs: `linux-headers`, `dkms`, modprobe configs, `nvidia_drm.modeset=1` in GRUB,
and runs `dkms install` for the running kernel (AUR package alone often leaves the module as “added” only).

Wiki: https://wiki.archlinux.org/title/NVIDIA

### 2) Hyprland 0.55+ config priority

Wiki: https://wiki.hypr.land/Configuring/

- Autogenerated `~/.config/hypr/hyprland.lua` is preferred over `.conf` if both exist.
- Installer moves `.lua` aside and installs this rice’s `hyprland.conf`.
- Removes deprecated `dwindle:pseudotile` (removed in Hyprland 0.55).

### 3) Waybar icons

- Package: `ttf-jetbrains-mono-nerd` (official `nerd-fonts` group).
- CSS font: `JetBrainsMono Nerd Font`. Without it, icons render as empty boxes.

---

## Install modes

| Command | What it does |
|---------|----------------|
| `./install.sh` | Auto: defaults + one confirm |
| `./install.sh --auto` | Fully non-interactive |
| `./install.sh --interactive` | Choose apps / shortcuts |
| `./install.sh --configs` | Symlink configs only |
| `./install.sh --packages` | Packages + NVIDIA only |
| `./install.sh --help` | Help |

After the first install, prefer the Waybar **Updates** module to pull rice changes: it shows notes from `UPDATE_NOTES.md`, asks before applying, then runs `git pull --ff-only` + `./install.sh --configs`. You can still run `--configs` by hand anytime.

---

## Defaults (auto mode)

| Role | App | Shortcut |
|------|-----|----------|
| Terminal | Kitty | `Super + T` |
| Browser | Firefox | `Super + B` |
| File manager | Thunar | `Super + E` |
| Launcher | Rofi | `Super + Space` |
| Lock | Hyprlock | `Super + L` |

Optional (on by default): Waypaper (AUR), Btop, nwg-look.  
greetd: off by default (enable in `--interactive`).

---

## What gets installed

### Pacman (core)

Hyprland stack, Waybar, SwayNC, PipeWire, Blueman, portals, fonts (`ttf-jetbrains-mono-nerd`),
`linux-headers`, `dkms`, chosen terminal/browser/FM/launcher.

### AUR (via yay — auto-installed if missing)

- `oh-my-zsh-git`, `zsh-theme-powerlevel10k-git`
- `waypaper-git` (if enabled)
- `nvidia-580xx-dkms` + `nvidia-580xx-utils` (Pascal / older only)

### Configs linked / copied

`hypr/`, `waybar/`, `rofi/`, `kitty/`, `swaync/`, `waypaper/`, `~/.local/share/bin/`,
`.zshrc`, `.bashrc`. `hyprland.conf` is copied and patched (not symlinked).

---

## After install / troubleshooting

1. **Always reboot** after NVIDIA DKMS install.
2. `nvidia-smi` fails →  
   `sudo ~/dotfiles/nvidia/setup-nvidia.sh && sudo reboot`
3. Wrong Hyprland keybinds / no Waybar → log out/in (remove leftover `hyprland.lua` if any).
4. Empty Waybar icons → `sudo pacman -S ttf-jetbrains-mono-nerd && fc-cache -fv`

Standalone NVIDIA script uses the same Arch News guidance as the main installer.

---

## Keyboard shortcuts (full)

See also the tables in [README.md](README.md).

| Shortcut | Action |
|----------|--------|
| `Super + T` | Terminal |
| `Super + B` | Browser |
| `Super + E` | File manager |
| `Super + Space` | App launcher |
| `Super + L` | Lock |
| `Super + C` | Close window |
| `Super + Shift + C` | Force kill |
| `Super + V` | Toggle floating |
| `Shift + F11` | Fullscreen |
| `Super + 1…0` | Workspaces 1–10 |
