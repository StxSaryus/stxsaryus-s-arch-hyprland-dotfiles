# ============================================================
# --- StxSaryus EDITION v1.0 | Arch Linux | NVIDIA 1050 Ti ---
# ============================================================

# 1. MONITOR (1920x1080 @ 60Hz)
monitor=,highres,auto,1

# 2. NVIDIA CRITICAL SETTINGS (Hyprland 0.53+)
env = LIBVA_DRIVER_NAME,nvidia
env = XDG_SESSION_TYPE,wayland
env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = NVD_BACKEND,direct
env = ELECTRON_OZONE_PLATFORM_HINT,auto
env = MOZ_ENABLE_WAYLAND,1

# Qt Wayland + multimedia (for Qt apps and media issues)
env = QT_QPA_PLATFORM,wayland;xcb
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = QT_AUTO_SCREEN_SCALE_FACTOR,1
env = QT_QPA_PLATFORMTHEME,gtk3

# System-wide dark theme (GTK3/4 + portals)
env = GTK_THEME,adw-gtk3-dark
env = GTK_ICON_THEME_NAME,Papirus-Dark
# Prefer dark for libadwaita / portals
exec-once = ~/.config/hypr/apply-dark-theme.sh

cursor {
    no_hardware_cursors = true
}

# Suppress start-hyprland warning (won't appear if greetd uses start-hyprland)
misc {
    disable_watchdog_warning = 1
}

# 3. AUTOSTART (only essentials)
# Sync waypaper's last choice to hyprpaper.conf so wallpaper persists across reboots
exec-once = ~/.config/hypr/wallpaper-sync.sh
exec-once = hyprpaper
exec-once = systemctl --user start xdg-desktop-portal xdg-desktop-portal-hyprland xdg-desktop-portal-gtk
exec-once = waybar -c ~/.config/waybar/config.jsonc -s ~/.config/waybar/style.css
exec-once = bash -c '~/.config/hypr/waybar-autohide.sh &'
exec-once = swaync

# 4. INPUT & KEYBOARD (English US)
input {
    kb_layout = us
    follow_mouse = 1
    touchpad {
        natural_scroll = true
    }
}

# 5. APPEARANCE (Elegant & modern)
general {
    gaps_in = 5
    gaps_out = 10
    border_size = 2
    col.active_border = rgba(33ccffee) # Arch Mavisi
    col.inactive_border = rgba(595959aa)
    layout = dwindle
}

decoration {
    rounding = 10
    blur {
        enabled = false
        size = 3
        passes = 1
    }
    shadow {
        enabled = false # Disabled to reduce GPU load on 1050 Ti
    }
}

# 6. ANIMATIONS (single clean block)
animations {
    enabled = true

    # Bezier curves (tuning smoothness)
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    bezier = bounce, 0.1, 1, 0.1, 1.1

    # Standard animations
    animation = windows, 1, 5, bounce, slide
    animation = windowsIn, 1, 5, bounce, slide
    animation = windowsOut, 1, 5, default, popin 80%
    animation = border, 1, 10, default
    animation = fade, 1, 5, default
    animation = workspaces, 1, 5, myBezier, slide
    animation = specialWorkspace, 1, 5, myBezier, slidevert
}

# 7. SHORTCUTS (SUPER = Win key)
$mainMod = SUPER

bind = SHIFT, F11, fullscreen
bind = $mainMod, T, exec, kitty
bind = $mainMod, C, killactive,
bind = $mainMod SHIFT, C, exec, hyprctl activewindow -j | jq -r '.pid' | xargs kill -9
# bind = $mainMod, M, exit,
bind = $mainMod, B, exec, firefox
bind = $mainMod, E, exec, thunar
bind = $mainMod, V, togglefloating,
bind = $mainMod, SPACE, exec, ~/.local/share/bin/launcher-toggle.sh
bind = $mainMod, L, exec, hyprlock

# --- NAVIGATION AND WORKSPACE MANAGEMENT ---

# A. Move focus with arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# B. Switch workspace with Super + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# C. Move window and follow it (Super + Shift + scroll)
# Note: When dragging a window, holding Shift moves your focus with it.
bind = $mainMod SHIFT, mouse_down, movetoworkspace, e+1
bind = $mainMod SHIFT, mouse_up, movetoworkspace, e-1

# D. Super + number to change workspace (1-9, 0 = 10)
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# E. Super + Alt + number to send window (silently)
bind = $mainMod ALT, 1, movetoworkspacesilent, 1
bind = $mainMod ALT, 2, movetoworkspacesilent, 2
bind = $mainMod ALT, 3, movetoworkspacesilent, 3
bind = $mainMod ALT, 4, movetoworkspacesilent, 4
bind = $mainMod ALT, 5, movetoworkspacesilent, 5
bind = $mainMod ALT, 6, movetoworkspacesilent, 6
bind = $mainMod ALT, 7, movetoworkspacesilent, 7
bind = $mainMod ALT, 8, movetoworkspacesilent, 8
bind = $mainMod ALT, 9, movetoworkspacesilent, 9
bind = $mainMod ALT, 0, movetoworkspacesilent, 10

# F. Super + Shift + number to move window and follow it
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# G. Mouse move/resize window (SUPER + left/right click)
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# 8. LAYOUT MODES
dwindle {
    preserve_split = true
}


# --- HP Omen FN keys (brightness + audio) ---

# Brightness (Fn + sun keys) + OSD notification
bind = , XF86MonBrightnessUp,   exec, ~/.config/hypr/brightness-osd.sh up
bind = , XF86MonBrightnessDown, exec, ~/.config/hypr/brightness-osd.sh down

# Audio (Fn + speaker keys)
bind = , XF86AudioRaiseVolume,  exec, pamixer -i 5
bind = , XF86AudioLowerVolume,  exec, pamixer -d 5
bind = , XF86AudioMute,         exec, pamixer -t
bind = , XF86AudioMicMute,      exec, pamixer --default-source -t

# Media keys (play / next / prev / stop)
bind = , XF86AudioPlay,   exec, playerctl play-pause
bind = , XF86AudioPause,  exec, playerctl play-pause
bind = , XF86AudioNext,   exec, playerctl next
bind = , XF86AudioPrev,   exec, playerctl previous
bind = , XF86AudioStop,   exec, playerctl stop
exec-once = nm-applet --indicator
