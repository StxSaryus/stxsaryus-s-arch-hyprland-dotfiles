# NVIDIA DRM modesetting (required for Hyprland/Wayland)
options nvidia_drm modeset=1

# Keep firmware loaded for faster resume on hybrid laptops
options nvidia NVreg_PreserveVideoMemoryAllocations=1
